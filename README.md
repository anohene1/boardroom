# Boardroom Assessment Project

### Install dependencies

```bash
npm install
```

### Run the development server

```bash
npm run dev
```

### Open application

Once the server is running, you should be able to open the app in your browser on localhost with port 3000 if no other application is using the port 3000

```
http://localhost:3000
```

